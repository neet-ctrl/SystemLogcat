<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
  <uses-permission android:name="android.permission.READ_LOGS"/>
  <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"/>
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
  <uses-permission android:name="moe.shizuku.manager.permission.API_V23"/>
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
  <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>

  <!-- Security Auditor permissions -->
  <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
      tools:ignore="ProtectedPermissions"/>
  <uses-permission android:name="android.permission.GET_TASKS"/>
  <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES"/>

  <queries>
    <package android:name="moe.shizuku.privileged.api" />
  </queries>

  <application
      android:label="@string/app_name"
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:hardwareAccelerated="true"
      android:theme="@android:style/Theme.Material.NoActionBar">

    <!-- Main launcher: System Console home (Shizuku setup + Security Auditor entry) -->
    <activity
        android:name="juloo.sysconsole.MainActivity"
        android:label="@string/app_name"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <!-- Security Dashboard — opened from MainActivity "Open Security Auditor" button -->
    <activity
        android:name="juloo.sysconsole.SecurityDashboardActivity"
        android:label="Security Auditor"
        android:exported="false"/>

    <!-- Scheduled scan via AlarmManager -->
    <receiver android:name="juloo.sysconsole.ScheduledScanReceiver"
        android:exported="false"/>

    <!-- Security Auditor: real-time install monitoring -->
    <receiver android:name="juloo.sysconsole.AppInstallReceiver"
        android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.PACKAGE_ADDED"/>
        <action android:name="android.intent.action.PACKAGE_REMOVED"/>
        <action android:name="android.intent.action.PACKAGE_REPLACED"/>
        <data android:scheme="package"/>
      </intent-filter>
    </receiver>

    <!-- Other Security Auditor screens -->
    <activity android:name="juloo.sysconsole.AppsListActivity"
        android:exported="false"/>
    <activity android:name="juloo.sysconsole.AppDetailsActivity"
        android:exported="false"/>
    <activity android:name="juloo.sysconsole.MonitorActivity"
        android:exported="false"/>
    <activity android:name="juloo.sysconsole.AlertsActivity"
        android:exported="false"/>
    <activity android:name="juloo.sysconsole.SecuritySettingsActivity"
        android:exported="false"/>

    <!-- Floating overlay service -->
    <service
        android:name="juloo.sysconsole.SysConsoleService"
        android:exported="false"
        android:stopWithTask="false"/>

    <!-- Shizuku provider (required by Shizuku SDK) -->
    <provider
        android:name="rikka.shizuku.ShizukuProvider"
        android:authorities="${applicationId}.shizuku"
        android:multiprocess="false"
        android:enabled="true"
        android:exported="true"
        android:permission="android.permission.INTERACT_ACROSS_USERS_FULL"/>

    <!-- FileProvider for PDF export sharing -->
    <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.provider"
        android:exported="false"
        android:grantUriPermissions="true">
      <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/provider_paths"/>
    </provider>

  </application>

</manifest>
