# This workflow has been merged into sysconsole-build.yml
# It is intentionally disabled — do not trigger it.
name: _disabled_security_auditor_ci

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. See sysconsole-build.yml instead."
